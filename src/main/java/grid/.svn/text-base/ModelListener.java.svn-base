package grid;

public interface ModelListener {

	/**
	 * Indicates that an event has been fired.
	 * @param eventName If caused by an action, format is fully.qualified.ActionClassName.eventName
	 * @param source The ModelObject which fired the event
	 */
	public void eventFired(String eventName, ModelObject source);
}
